{
  "name": "roadroller",
  "version": "1.0.0",
  "keywords": ["loadtest", "testing"],
  "license": "ISC",
  "description": "load testing tool",
  "author": "zodiac-G12",
  "type": "module",
  "main": "src/main.ts",
  "engines": {
    "node": ">=22.5.1",
    "npm": "npmではなくpnpmを使用してください",
    "yarn": "yarnではなくpnpmを使用してください",
    "pnpm": ">=9.9.0"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "prepare": "husky",
    "build": "tsc",
    "start": "ts-node src/main.ts && tee -a logs/${date +%Y-%m-%d-%H-%M-%S}.log",
    "type-check": "tsc --noEmit",
    "lint-staged": "lint-staged",
    "format": "biome format --write ./src",
    "lint": "biome lint ./src"
  },
  "lint-staged": {
    "*.{js,json,md,yml,ts,tsx,jsx,css,scss}": ["pnpm format", "pnpm lint"]
  },
  "devDependencies": {
    "@types/node": "^22.5.3",
    "@biomejs/biome": "1.8.3",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.3",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4",
    "loadtest": "^8.0.9"
  }
}
